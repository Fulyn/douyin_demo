<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/video_background">

    <include layout="@layout/view_video_player" />

    <!-- 网络首图层：没看过的视频用 coverUrl 露出第一帧海报（Fresco 懒加载+缓存），加载前为黑底。 -->
    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@+id/video_cover_poster"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/video_background"
        android:visibility="gone"
        fresco:actualImageScaleType="fitCenter" />

    <!-- 截帧封面层（盖在首图之上）：看过的视频用上次画面，切走/返回更无缝。 -->
    <ImageView
        android:id="@+id/video_cover"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/video_background"
        android:scaleType="fitCenter"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/video_pause_icon"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_gravity="center"
        android:alpha="0.72"
        android:src="@drawable/pause_icon"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingEnd="96dp"
        android:paddingBottom="34dp">

        <TextView
            android:id="@+id/video_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="start"
            android:textColor="#FFFFFFFF"
            android:textSize="21sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/video_meta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="start"
            android:textColor="#D9FFFFFF"
            android:textSize="14sp" />
    </LinearLayout>

    <FrameLayout
        android:id="@+id/video_progress_track"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_gravity="bottom"
        android:layout_marginBottom="10dp"
        android:background="#5C5C5C"
        android:visibility="gone">

        <View
            android:id="@+id/video_progress_fill"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/white"
            android:scaleX="0"
            android:transformPivotX="0dp" />
    </FrameLayout>

    <View
        android:id="@+id/video_progress_touch"
        android:layout_width="match_parent"
        android:layout_height="34dp"
        android:layout_gravity="bottom"
        android:layout_marginBottom="10dp" />
</FrameLayout>
